#!/bin/sh -e

name="$1"

if [ -z "$name" ]; then
  echo "One argument expected: module name"
  exit 1
fi

if [ -f "./$name" ]; then
  rm "./$name"
fi

go build "$name" >/dev/null

for i in {1..3}; do
  echo "-------------------------"

  echo "Run $i:"
  time "./$name" 2>/dev/null
done
